export default async function sentryLoader() {
  // Sentry is now initialized in instrumentation.ts via the register() hook.
  // This loader remains as a placeholder to prevent import/loading issues but does nothing.
}

